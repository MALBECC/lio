#/bin/bash
################################################################################
DELAMBER ()
{
   sysname="$1"

   rm -f ${sysname}.stdo
   rm -f ${sysname}.out
   rm -f ${sysname}.rst7
   rm -f ${sysname}.mdcrd
   rm -f ${sysname}.mdfrz
   rm -f ${sysname}.mdvel
}
################################################################################
# RUN ORDER
################################################################################
DELAMBER "water_scratch"
DELAMBER "water_restart"
################################################################################
